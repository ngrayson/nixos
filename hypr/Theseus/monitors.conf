# Theseus (Framework 13 AMD AI 300) — starting internal-panel layout.
# Capture the live layout on-device with `hyprmon-cfg` (uses $(hostname)),
# then rebuild. Do not copy hypr/Tawa/monitors.conf (desktop DP/HDMI).
#
# 2880x1920 @ scale 1.6 ≈ 1800x1200 logical. If the panel feels too large
# or too small, try 1.5 (1920x1280) via hyprmon-cfg.

monitor=eDP-1,2880x1920@120,0x0,1.6
