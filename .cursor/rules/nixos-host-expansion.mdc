---
description: Safely add or modify NixOS hosts, shared modules, and profiles
globs: "{flake.nix,hosts/**/*.nix,common/**/*.nix,profiles/**/*.nix}"
alwaysApply: false
---

# NixOS host expansion

- Treat `flake.nix` and `flake.lock` as the only source of external inputs; do not add channel paths, `builtins.fetchTarball`, or unlocked `builtins.getFlake` calls.
- Add a host under `hosts/<Name>/` with `configuration.nix`, `host.nix`, and that machine's generated `hardware-configuration.nix`.
- Add the case-sensitive host output to `nixosConfigurations` in `flake.nix`; put `nixos-hardware` modules in its module list.
- Never copy hardware UUIDs, LUKS mappings, swap devices, or resume offsets between machines.
- Set `system.stateVersion` once in `host.nix` to the machine's initial install release; do not bump it during upgrades.
- Put server-safe defaults in `common/base.nix`. Import desktop applications, display managers, gaming, VPN, and Home Manager only from workstation profiles.
- Validate a host in this order: `nix flake check --no-build`, `os-rebuild build --host <Name>`, then `os-rebuild dry-activate --host <Name>`.
- Prefer `boot` for kernel, initrd, disk, LUKS, or hibernation changes. Ask the user before rebooting or restarting their graphical session.
- Do not activate a host whose placeholder hardware configuration still contains all-zero UUIDs.
